{
  "name": "@local/playground",
  "description": "Web app for seeing how Raygun dashboard reacts to events, with this client.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "svelte-kit dev --host ${HOST:-localhost} --port ${PORT:-4000}",
    "//build": "svelte-kit build",
    "//preview": "svelte-kit preview",
    "lint": "eslint ."
  },
  "dependencies": {
    "@local/rg4esm": "file:../package"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "eslint": "^8.8.0",
    "eslint-plugin-svelte3": "^3.4.0",
    "mdsvex": "^0.9.8",
    "svelte": "^3.46.3"
  }
}
